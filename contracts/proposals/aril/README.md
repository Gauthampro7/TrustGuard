# aril's interface requests

Copy ../TEMPLATE.md to <task-id>.md here. Include exact inputs/outputs, signatures or endpoints, units, bounds, unavailable/error behavior, compatibility and consumer tests. Continue against v1 until Gautham publishes an accepted contract. This directory is yours; shared contracts and other modules are not.
